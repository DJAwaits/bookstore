extends ../layout

block content
    h3= book.name
    a(href=`/courses/edit/${course.id}`) Edit Book
    div
        p Author: #{book.author}
        p Publisher: #{book.publisher}
        p Genre: #{book.genre}
        p Page Count #{book.pageCount}
        p Cover: #{book.cover}
        p= book.description